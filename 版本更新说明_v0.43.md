# v0.43 版本更新说明｜正式版

> **版本状态：正式版 / 公开共创**  
> **发布日期：2026-08-17**

## 1. 默认不再要求用户说“调用爽律”

支持 Agent Skills 自动发现的宿主可根据 `SKILL.md` metadata 在实质性法律专业任务中自动加载爽律。旧别名继续兼容，并作为永久保底入口。

## 2. 自动激活属于个性化

`activation.mode` 支持 `AUTO / CONFIRM / MANUAL`。用户一旦明确选择，后续升级按 `PRESERVE_USER` 保留，不影响既有模板、文书风格、研究报告格式等个性化资产。

## 3. 自动激活不等于关键词触发

明确法律专业任务优先自动进入；普通写作、日常问题、简单文件处理或仅有法律词汇重叠不得误触发。工程层保留正反触发评测集。

## 4. 多 Skill 协同

爽律不绑定具体 OCR、数据库、Office 或 Web Skill。需要外部能力时形成 `CapabilityRequirement / SkillCollaborationRequest`，由当前 Agent 选择可用实现；爽律继续控制法律分析、来源、事实状态和质量门。

## 5. 旧版能力兼容与失败回退

- 新增 Legacy Compatibility Contract；
- 新增独立 Feature Flags；
- 显式“调用爽律”绕过 AutoActivation；
- Router v2 失败时回退 v0.42 路由；
- 主动建议、多 Skill 协同失败不得阻断旧核心任务；
- 新增 `GATE-LEGACY-COMPAT` 与 Legacy Regression。

**正式版发布底线：新功能失败只允许损失新功能，不允许拖死 v0.42 已经能够完成的核心任务。**

## 6. 包装规范

Skill 根目录标准化为 `shuanglv-legal-skills`，frontmatter `name` 与目录名一致。中文品牌名称继续作为显示名称和发布包名称。

## 7. 开发阶段历史

- `v0.43-alpha1`：完成 Agent Native 自动激活、激活偏好、多 Skill 协同和主动建议的第一轮工程实现；
- `v0.43-alpha2`：补齐 Feature Flags、显式调用旁路、Router v2 故障回退和 Legacy Regression；
- `v0.43`：完成正式发布收口，冻结上述兼容底线，后续真实反馈进入后续版本。
