# 参与共创指南 · Contributing

感谢你愿意为 **爽律 Skill｜律师专业工作流系统** 出力。本文说明可以贡献什么、怎么提交、我们如何评审，以及一份合格改动应当满足的约定。

> 一句话原则：**爽律 Skill 是给律师用的专业工作流，改动要经得起律师同行的对抗性审查。**

---

## 一、可以贡献什么

| 类型 | 举例 | 建议入口 |
|---|---|---|
| 问题反馈 | 某条业务规则与现行法律实务不符；某个门控过严/过松；某平台加载不了入口 | Issue（问题反馈模板） |
| 文字修订 | 错别字、歧义表述、术语不统一、链接失效 | 直接 PR |
| 检查清单 / 对抗性审查清单补充 | 某技能漏了一个常见的反向审查点 | 直接 PR（附理由） |
| 数据结构（schema）完善 | 字段缺失、约束不合理、`$id` 版本管理 | 先开 Issue，再 PR |
| 新业务模块 / 新技能 | 例如"行政争议""劳动争议""知识产权"专项 | **先开 Issue 讨论范围与边界**，达成一致后再 PR |
| 使用示例 | 新的端到端示例、跨技能组合示例 | 直接 PR |
| 平台适配 | 某个 Agent 平台的入口注册方式 | 直接 PR，只改入口 / 能力声明，不改业务规则 |

**不接受**的改动：

- 为适配某个平台而修改法律业务规则本身；
- 把系统改成"自动给答案"、削弱人工复核与授权门控的改动；
- 含任何真实案件信息的内容（见第四节）。

---

## 二、提交方式

### 路径 A · 开 Issue（无需 Git）

点击 [New issue](../../issues/new/choose)，选择模板填写即可。维护者会据此跟进，必要时代为整理成 PR。

### 路径 B · Pull Request

1. Fork 本仓库，从 `main` 切一个分支（建议命名 `fix/…`、`docs/…`、`skill/…`、`schema/…`）；
2. 按第三、四节的约定修改；
3. 提交前过一遍 PR 模板里的自检清单；
4. 发起 PR，描述里写清 **改了什么、为什么、影响哪些技能/文件**；涉及业务规则的，请附上你的专业依据（法条 / 司法解释 / 实务惯例 / 反例）。

小改动一个 PR 一件事；大改动先开 Issue 拆分。

---

## 三、目录与文件约定

### 3.1 顶层结构

```text
SKILL.md                 Agent 机器入口（别名路由 → 00_使用与调度/爽律 Skill入口.md）
manifest.json            系统元数据：system_id / version / skills / 策略文件指针
00_使用与调度/            入口、调度规则、统一对抗性审查规范、人工决策与升级矩阵、项目执行说明书
01_运行规范/              输入质量、运行能力契约、外部处理风险门控、交付物输出与文书样式规范
02_公共接口/              公共接口规范 + 数据结构/（跨技能共享的 JSON Schema）
03_技能/<技能名>/          业务技能
04_使用示例/              端到端示例
```

### 3.2 每个业务技能的标准文件

```text
03_技能/<技能名>/
├── 技能定义.md              必有。含 `skill_id`、用途、适用场景、能力边界、输入输出、总流程
├── 详细说明与工作流程.md      必有。给人看的完整方法地图（人工核验第一入口）
├── 核心规则汇总.md           必有。必须遵守的规则、门控和禁止事项
├── 检查清单汇总.md           必有。执行完成前的质量控制
├── 对抗性审查清单.md         必有。该技能专属的第二遍审查
├── 业务模块汇总.md           可选。每一步具体如何执行
└── 数据结构/                可选。该技能扩展的 JSON Schema
```

新增技能时，**必须同步更新**：

- `manifest.json` 的 `skills`（可选技能同时加进 `optional_skills`）；
- `00_使用与调度/技能总览.md`；
- `00_使用与调度/技能调度规则.md`（什么任务路由到它、与哪些技能组合）；
- `README.md` 第二节技能表。

### 3.3 数据结构（JSON Schema）

- 使用 JSON Schema **2020-12**；
- 每个 schema 带 `$id`（形如 `https://legal-skill.local/schema/<name>/<major.minor>`）与 `title`；
- 顶层带 `schema_version` 字段并用 `const` 锁定；
- **不向后兼容的改动必须递增 `$id` 与 `schema_version` 的版本号**，并在 PR 中说明迁移方式；
- 跨技能共享的放 `02_公共接口/数据结构/`，只在某技能内使用的放该技能的 `数据结构/`。

### 3.4 写作规范

- 语言：简体中文（zh-CN）；术语与现行法律法规、司法解释一致；
- 名称：说明正文统一用 **"爽律 Skill"**；只有用户调用示例中才单用"爽律"；
- 语气：写给执业律师看的专业说明，克制、可核验；不写营销话术；
- 事实分层：规则文本中凡涉及"事实 / 陈述 / 推断 / 未知"，保持四者区分的表述；
- 引用：法律依据写清名称与条文；不写具体日期或"现行"字样以外无法核验的断言。

---

## 四、脱敏与保密（硬性）

本仓库公开可见。**任何文件、示例、Issue、PR、commit message 中，都不得出现：**

- 真实案号、真实当事人 / 证人 / 被害人姓名、身份证号、手机号、地址、银行卡号；
- 真实的案件文件名、卷宗页码引用、客户名称、交易对手名称；
- 未公开的侦查、诉讼、仲裁或交易材料内容；
- 律所内部未公开的模板、收费、客户信息。

示例一律使用占位符：`张三 / 李四`、`某某公司`、`（2024）粤0XXX民初XXXX号`、`<docId>`。

提交前建议自查：

```bash
grep -rnoE '[（(][12][0-9]{3}[）)][^号]{1,30}号' .     # 案号
grep -rnoE '\b[0-9]{17}[0-9Xx]\b|\b1[3-9][0-9]{9}\b' .   # 身份证 / 手机号
```

如已误提交，请立即联系维护者——公开仓库需要重写历史才能真正抹除。

---

## 五、评审流程

1. **维护者初审**（结构 / 格式 / 脱敏 / 平台适配 / 是否同步更新了关联文件）；
2. **作者终审**（凡涉及法律业务规则、门控、对抗性审查点、人工升级边界的改动）；
3. 通过后 squash 合并到 `main`，记入 `CHANGELOG.md`；
4. 累积到一定程度打 tag 发布新版本，并同步 `manifest.json` 的 `version`。

评审重点是**专业正确性与可核验性**，其次才是表述。有分歧时以作者意见为准；无法达成一致的提案会以 Issue 形式保留讨论记录。

---

## 六、贡献许可

向本仓库提交内容（Issue、PR、评论中的建议文本），即视为你：

- 确认该内容为你原创或你有权按本仓库许可提供；
- 同意该内容按本仓库届时生效的许可（见 `README.md` 第十一节与 `许可与使用声明.md`）对外发布；
- 授权维护者与作者合并、修改、再分发。

---

## 七、行为准则

对事不对人；对专业问题保持对抗性，对人保持尊重。涉及具体案件的讨论请脱敏后进行。
