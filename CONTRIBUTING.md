# CONTRIBUTING｜共创与贡献指南

感谢参与爽律 Skill 共创。本项目希望把律师专业方法沉淀为可执行、可组合、可核验的 Agent Skill；贡献的重点是提高真实法律工作中的准确性、完整性、推理质量和可执行性，而不是堆叠功能数量。

## 一、适合提交什么

优先欢迎以下贡献：

1. **真实失败反馈**：说明任务背景、期望行为、实际行为和失败点；真实材料必须脱敏。
2. **工作流补缺**：指出某业务场景缺失的阶段、角色、证据、程序或策略动作。
3. **分析与推理改进**：改进事实建模、问题构造、证据检验、规则适用、竞争解释、策略选择和论证表达。
4. **外部能力适配**：改进法律数据库、用户知识库、文件读取、OCR、Office、Web 或其他 Agent 工具的厂商无关调用规则。
5. **工程约束**：改进路由、渐进加载、动态清单、Schema、质量门控、全链路溯源和回写机制。
6. **文档与示例**：提升说明清晰度，补充不含真实隐私或第三方受限内容的示例。

## 二、不建议直接提交什么

- 大量静态法条、案例全文或容易过时的现行法结论；
- 仅适用于单一案件、无法抽象的方法；
- 复制其他 Skill、产品、书籍或文章的表达、目录或结构；
- 用主观百分比冒充胜诉率、定罪率或法律置信度；
- 无法说明来源的法律规则或案例结论；
- 未经授权的客户材料、付费数据库内容或第三方受保护文本；
- API 密钥、账号、Token 等凭证。

## 三、建议的 Issue / PR 说明结构

提交改进时，尽量说明：

- **问题是什么**：在哪个任务、角色、阶段出现；
- **为什么是问题**：会造成什么法律或执行风险；
- **建议怎么改**：新增、删除还是调整哪条规则；
- **影响范围**：哪些 Skill、Schema、检查清单或质量门控受影响；
- **依据是什么**：如涉及法律规则、案例或事实性判断，提供可核验来源；如属于方法论，说明适用场景和边界；
- **是否需要回归检查**：哪些既有流程可能被改坏。

## 四、贡献的专业质量要求

- 事实与推断分开；
- 法律规则与个案适用分开；
- 支持观点与反向观点均允许进入分析；
- “未检索到”不得自动写成“不存在”；
- 案例应比较决定性事实和区别点，而不是只比关键词；
- 涉及现行法时，应优先核验官方或其他可信权威来源；
- 新规则应尽量写成“什么时候触发、如何执行、何时失败/升级、输出什么”，避免只写口号。

## 五、版权、许可和署名

提交贡献即表示你确认：

- 你有权提交相应内容；
- 贡献不包含你无权再许可的第三方受保护材料；
- 文本性 Skill 内容按项目的 CC BY-SA 4.0 许可分发；
- 程序性文件按 Apache-2.0 许可分发；
- 无需向项目转让你对原创贡献本身享有的其他权利，但项目和后续使用者可以依对应许可证使用、修改和分发该贡献。

## 六、隐私与案件材料

请不要为了复现问题直接上传真实完整卷宗。优先采用：

1. 去标识化后的最小片段；
2. 自行构造但保持问题结构的模拟材料；
3. 只描述失败条件和预期输出；
4. 必要时仅在安全、受控渠道下向有权限的维护者提供信息。

公开仓库中的任何内容都应按“可能被长期公开、复制和索引”处理。

<!-- repo-appendix:start ｜ 仓库层，由 .github/scripts/apply_repo_layer.py 从 .github/repo-layer/CONTRIBUTING.appendix.md 生成；不要手改本段。以上为作者原文。 -->

---

## 七、仓库操作细则（维护者补充）

以上一至六节是作者对"贡献什么、质量要求、许可与隐私"的规定，以其为准。本节只补充在 GitHub 仓库里**怎么操作**。

### 7.1 两条路径

- **开 Issue（无需 Git）**：[New issue](../../issues/new/choose) 选模板——「真实失败反馈」「问题反馈」「提案」；模板字段就是第三节要求的说明结构。
- **Pull Request**：Fork → 从 `main` 切分支（建议 `fix/…`、`docs/…`、`skill/…`、`schema/…`）→ 修改 → 过一遍 PR 模板自检 → 发起 PR。小改动一个 PR 一件事；新技能、新模块、不向后兼容的 schema 变更请先开「提案」Issue 讨论范围与边界。
- 作者与仓库维护者（组织 Owner）可直接提交到 `main`；作者提交新版时请先运行 `python3 .github/scripts/apply_repo_layer.py`，README / CONTRIBUTING 的仓库层头尾不会丢。

### 7.2 目录与联动更新

- 业务技能目录标准文件：`技能定义` / `详细说明与工作流程` / `分析与推理方法` / `核心规则汇总` / `检查清单汇总` / `对抗性审查清单`，可选 `业务模块汇总` 与 `数据结构/`。
- **新增或重命名技能时须同步**：`manifest.json`（`skills` / `optional_skills`）、`00_使用与调度/技能总览.md`、`00_使用与调度/技能调度规则.md`、README 技能表，以及 `05_工程执行层/配置/` 下按技能名索引的 `加载清单.json` 与 `动态清单注册表.json`。
- 平台适配只改入口与能力声明（`SKILL.md`、`01_运行规范/` 能力契约），不为适配平台改法律业务规则本身。

### 7.3 JSON Schema 约定

- JSON Schema 2020-12；每个 schema 带 `$id`（`https://legal-skill.local/schema/<name>/<major.minor>`）与 `title`，顶层 `schema_version` 用 `const` 锁定。
- **不向后兼容的改动必须递增 `$id` 与 `schema_version` 版本号**，并在 PR 中说明迁移方式；跨技能共享的放 `02_公共接口/数据结构/`，仅某技能使用的放该技能的 `数据结构/`。
- 程序性文件（`.py` / `.json` / schema）按 Apache-2.0；改动执行器请保持"仅标准库、不联网、不写入仓库外路径"。

### 7.4 写作与脱敏自查

- 说明正文统一用"爽律 Skill"，只有用户调用示例中才单用"爽律"；简体中文，术语与现行法律法规一致。
- 提交前自查（PR 模板里也有）：

```bash
grep -rnoE '[（(][12][0-9]{3}[）)][^号]{1,30}号' .            # 真实案号
grep -rnoE '\b[0-9]{17}[0-9Xx]\b|\b1[3-9][0-9]{9}\b' .      # 身份证 / 手机号
grep -rnoiE '(sk|key|token|secret)[-_ ]?[a-z0-9]{20,}' .      # 凭证
```

如已误提交真实材料，请立即联系维护者——公开仓库需要重写历史才能真正抹除。

### 7.5 评审与发布

1. 维护者初审：结构、格式、脱敏、平台适配、联动文件是否同步；
2. 作者终审：凡涉及法律业务规则、门控、对抗性审查点、人工升级边界的改动；有分歧以作者意见为准；
3. 通过后 squash 合并到 `main`，记入 `CHANGELOG.md`；`main` 禁止强推与删除，非 Owner 的改动一律经 PR；
4. 作者交付的每个版本按原样导入（或由作者自提 PR 合并）并打 tag；仓库层（README / CONTRIBUTING 头尾、`.github/`、`CHANGELOG.md`）与作者正文分层维护，合并作者新版后运行 `python3 .github/scripts/apply_repo_layer.py` 即可套回，见 `.github/MAINTAINING.md`。

<!-- repo-appendix:end -->
