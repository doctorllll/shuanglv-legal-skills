# 新功能失败回退示例

用户说：“**调用爽律审查这个合同，沿用我的合同审查模板。**”

即使 v0.43 的 AutoActivation 配置损坏，系统仍应：

1. 识别“调用爽律”为显式保底入口；
2. 绕过自动激活判断；
3. 使用 v0.42 已验证路由进入“合同与交易工作”；
4. 继续解析用户已有模板资产，不恢复系统默认模板；
5. 正常完成合同审查。

允许损失的是“自动、无感调用”这一增强体验，不允许损失原有合同审查能力。
